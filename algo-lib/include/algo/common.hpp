#pragma once
// ============================================================================
// algo/common.hpp —— 公共基础定义
// ----------------------------------------------------------------------------
// 功能：为整个模板库提供共享的类型别名、常量与通用结构。
//       各分类头文件按需 #include "algo/common.hpp"，不要重复定义同名实体。
// 时间/空间复杂度：无运行时开销（均为编译期常量与内联结构）。
// ============================================================================
namespace algo {

// 常用类型别名
using ll = long long;
using ull = unsigned long long;

// 无穷大：图论最短路等场景的"不可达"初值。
// 注意：不要对两个 INF 做加法（会溢出），需要加边权时先判断 != INF。
constexpr ll INF = 0x3f3f3f3f3f3f3f3fLL;

// 浮点比较精度（计算几何统一使用）
constexpr double EPS = 1e-9;

// 通用带权边：用于 dijkstra / spfa / prim / lca 等图论模板
struct Edge {
    int to;
    ll w;
};

}  // namespace algo
